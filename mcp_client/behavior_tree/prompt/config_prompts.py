# 节点依赖原则
NODE_DEPENDENCY_PRINCIPLES="""
请按照**依赖矩阵四步法**生成行为树。

## 依赖矩阵四步法

### 第1步：列出所有任务
将用户需求分解为子任务，每个任务一个编号（T1, T2, T3...）。

**格式要求**：
T1: [任务描述]
T2: [任务描述]
T3: [任务描述]
...

### 第2步：分析依赖关系
填写依赖矩阵，回答两个核心问题：
1. **这个任务需要什么？**（目录、文件、数据、结果）
2. **这个任务依赖哪些任务？**（必须等谁完成）

**格式要求**（Markdown表格）：
| 任务 | 依赖哪些任务 | 依赖说明 |
|------|-------------|---------|
| T1   | 无          | 第一个任务，无依赖 |
| T2   | T1          | 需要T1创建的资源 |
| T3   | T1, T2      | 需要T1的资源和T2的数据 |
| ...  | ...         | ... |

### 第3步：构建依赖图
根据依赖关系，画出执行顺序：

**规则**：
- **无依赖的任务** → 可以最先执行
- **有依赖的任务** → 必须等依赖完成
- **相互独立的任务** → 可以并行执行


**格式要求**（文本依赖图）：
T1, T2 (无依赖，可并行)
├─ T3 (依赖T1)
├─ T4 (依赖T2)
└─ T5 (依赖T1和T2)
├─ T6 (依赖T5)
└─ T7 (依赖T3和T6)


### 第4步：生成行为树JSON
根据依赖图，构建树结构：

**转换规则**：
| 依赖关系                |     树结构    |      描述    |       示例           | 
| **顺序执行**  A → B → C | **Sequence** | 有明确先后依赖 | 先创建目录，再写入文件 |
| **并行执行**  A ∥ B ∥ C | **Parallel** | 互不依赖，可同时做 | 同时查询多个城市天气 |
| **条件选择**  A | B | C | **Selector** | 互斥方案，选一个执行 | 尝试A，失败则B，再失败则C |
| **条件+动作** if(C) then A else B | **Selector + Sequence + Condition** | 根据条件选择不同路径 | 如果有网络查在线，否则读缓存 |

选择节点类型的规则
├─ 任务之间有数据依赖吗？ → 是 → Sequence
└─ 任务之间相互独立吗？ → 是 → Parallel
   └─ 任务是互斥方案吗？ → 是 → Selector
      └─ 是混合型？ → 外层Sequence，内层灵活组合

生成时请逐项核对你的行为树：
### 依赖完整性检查
- 每个需要资源的任务，其依赖任务是否都列在依赖矩阵中？
- 是否存在循环依赖？（A依赖B，B依赖A）
- 所有任务是否都能从根节点到达？

### 树结构检查
- 有依赖的任务是否放在依赖任务之后？
- 相互独立的任务是否用 Parallel 并行执行？

### 节点精简检查
- 是否有工具可以一步完成的任务被拆成了多步？
- 是否存在不必要的中间节点？

"""

# 语义化节点ID示例
SEMANTIC_NODE_ID_EXAMPLES = """
### 目录和文件操作
{
  "type": "Sequence",
  "name": "准备工作",
  "id": "sequencePrepareWork",
  "children": [
    {
      "type": "Action",
      "name": "创建主目录",
      "id": "actionCreateMainDir",
      "tool": "file_operations",
      "parameters": {"operation": "create", "path": "abc"}
    },
    {
      "type": "Action",
      "name": "创建当前天气目录",
      "id": "actionCreateCurrentWeatherDir",
      "tool": "file_operations",
      "parameters": {"operation": "create", "path": "abc/def"}
    }
  ]
}
"""

# 行为树配置原则
BEHAVIOR_TREE_CONFIG_PRINCIPLES = f"""
行为树节点类型：
- Sequence: 序列节点（所有子节点按顺序执行，全部成功才返回成功）
- Selector: 选择器节点（依次执行子节点，直到有一个成功）
- Parallel: 并行节点（同时执行所有子节点，提高执行效率）
- Action: 动作节点（调用MCP工具）
- Condition: 条件节点（检查条件）
- Inverter: 反转装饰器（反转子节点状态：SUCCESS↔FAILURE）
- Timeout: 超时装饰器（限制子节点执行时间）
- Repeat: 重复装饰器（重复执行子节点指定次数）

## 语义化节点ID命名规则
- 使用**驼峰命名法**（如"createMainDir"、"queryBeijingWeather"）
- ID必须反映节点的名称与**功能**，且字段的值必须唯一，符合命名规范
- 后续节点引用时，直接使用这些语义ID

# 语义化节点ID示例
{SEMANTIC_NODE_ID_EXAMPLES} 

条件表达式必须使用python3的语法：
- 支持比较运算: ==, !=, >, <, >=, <=
- 支持逻辑运算: and, or, not
- 支持成员运算: in
- 支持函数调用: len(), str(), int()
- 引用前面结果: 语义化节点ID.result

条件表达式示例：
- "语义化节点ID.result == true"
- "len(语义化节点ID.result) > 5"
- "yyy in 语义化节点ID.result"
- "语义化节点ID.result in ['yyy', 'zzz']"

参数引用格式规则（必须严格遵守）：
1. 每个引用必须完整：{{{{语义化节点ID.result}}}}
2. 如果字符串中包含多个引用，确保每个引用都完整闭合

参数引用语法示例：
- 支持引用前面节点的结果: {{{{语义化节点ID.result}}}}
- 多节点引用示例："{{{{语义化节点ID1.result}}}} 和 {{{{语义化节点ID2.result}}}}"


重要规则：
1. 为每个节点指定**有意义的语义ID**，严格遵循命名规范
2. 参数引用时必须使用与节点ID完全匹配的语义ID
3. 根据用户输入生成合理的任务执行流程，且生成的行为树任务节点尽可能少
4. 使用合适的节点类型（Sequence、Selector、Parallel等）
5. 根据需要使用条件节点实现分支逻辑
6. 不要使用不存在的工具
7. {NODE_DEPENDENCY_PRINCIPLES}
"""




